package quill

// Version is the module's released semantic version, without the leading v. The
// release workflow requires the pushed tag to equal "v" + Version, so this
// constant is the single source of truth for the version the module reports.
const Version = "0.2.0"
